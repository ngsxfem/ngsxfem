\RequirePackage{luatex85}
\documentclass{standalone}

\usepackage{xcolor}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{amsthm}

\usepackage{tensor}
\usepackage{autonum}
\usepackage{tikz}
\usetikzlibrary{positioning,shapes,arrows,calc,angles}

\usepackage{lmodern}

\usepackage[T1]{fontenc}
\usepackage{polyglossia}
\setdefaultlanguage{english}
\usepackage{libertine}


\usepackage{pgfplots}
\usepgfplotslibrary{colorbrewer,patchplots}
\pgfplotsset{compat=1.15}

\usepackage{shellesc}

\newcommand\plotsquareiso[4]{
     \def\aa{#1}
     \def\ba{#2}
     \def\ab{#3}
     \def\bb{#4}
  
    \addplot3[
     contour filled={levels={0},labels=false}, 
    ]{\aa * (1-x) * (1-y)+ \ba * x * (1-y)+ \ab* (1-x) * y+ \bb * x * y};

    \addplot3[
     contour gnuplot={levels={0},labels=false, draw color=red},
     very thick,
    ]{\aa * (1-x) * (1-y)+ \ba * x * (1-y)+ \ab* (1-x) * y+ \bb * x * y};
    }

\begin{document}
\begin{tikzpicture}[scale=1,rotate=0,
 dt_neg/.style = {diamond, fill=blue,scale=0.3,draw},
 dt_pos/.style = {rectangle,fill=green!50!black,scale=0.4,draw},
 dt_if/.style  = {circle,fill=red,scale=0.4,draw}]
\begin{axis} [font=\tiny, at={(0,0)}, domain = 0:1, view = {0}{90}, colormap={my_colormap}{color=(white!75!blue), color=(white!75!green)}, ticks=none]
     \input{tikz_out}
\end{axis}

\end{tikzpicture}

\end{document}
