beforescript:
  stage: build
  script:
    - echo " before script "
    - use-netgen " which ngscxx "
    - export TEST_ENV_VAR="testtesttest"
    - export | grep TEST_ENV_VAR

build:
  stage: build
  script:
    - echo " testenvironment "
    - export | grep TEST_ENV_VAR
    - uname -a
    - ls -al
    - pwd
    - gcc --version
    - mkdir build
    - cd build
    - use-netgen --run "gcc --version"
    - use-netgen --run "cmake ../."
    - use-netgen --run "make"

test1:
  stage: test
  script:
    - echo " test1 "

deploy:
  stage: deploy
  script:
  - echo " deploy "

cleanup:
  stage: deploy
  script:
    - echo " cleanup "
    - pwd
